services:
  frontend:
    restart: always
    build:
      context: ./requirements/frontend
      dockerfile: Dockerfile
      # dev requires 5173:5173 / prod requires 80:80
      target: dev
    networks:
      - net
    ports:
      - "5173:5173"
networks:
  net:
    driver: bridge